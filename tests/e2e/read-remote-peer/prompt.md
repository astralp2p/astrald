An astral object is stored on astral node `node2`; its id is in
`~/peer-object.json` (the `peer_object_id` value). Read it from `node2`.

Save how many bytes you read to `~/read.json` (as `remote_read_bytes`).
